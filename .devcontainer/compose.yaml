services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - type: bind
        source: ..
        target: /workspace
    environment:
      - TZ=Asia/Tokyo
      - SKIP_DOCKER=y
      - SKIP_DENO=y
    command: sleep infinity
